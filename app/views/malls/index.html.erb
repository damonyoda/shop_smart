


<div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Malls in your area</h1>
                </div>

    <!-- /#wrapper -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            All malls
                        </div>
                        
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table id= "items" width="100%" class="table table-striped table-bordered table-hover">
                                
                                <thead>
                                    <tr>
        <th>Mall</th>
    </tr>
    </thead>
    <tbody>
<% @malls_list.each do |mall_name, mall| %>
<% mall.each do |i| %>
    <tr>
        <td><%= link_to i.mall_name, i %></td>
    </tr>
    <% end %>
    <% end %>
    <% if  current_user.try(:admin?) %>
    <%= link_to "New malls?", new_mall_path %>
    <% end %>
     </tbody>
    </table>
    </div>
    </div>
    </div>
    </div>



